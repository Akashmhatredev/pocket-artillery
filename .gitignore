node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Rust / SpacetimeDB module build artifacts
stdb-module/target/
stdb-module/tests-core/target/
# NOTE: module_bindings/ is intentionally COMMITTED so Vercel can build the
# frontend without the SpacetimeDB CLI. Regenerate it with `npm run stdb:generate`.
